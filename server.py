from socket import socket, AF_INET, SOCK_STREAM
from threading import Thread
import time

sock = socket(AF_INET,SOCK_STREAM)

conn_ids = {}

def handle_Data():
    global id_conuter
    while True


    for connl in playr_data:
        if connl in  eliminated: continue
        p1 = player_data

parts = data.splir